# Schemas

Schemas make records understandable to humans, CI, analysis scripts, and software agents.

The first schemas cover sessions and cohorts. Add new schemas only when data are actually collected.

Version schemas explicitly. Breaking field changes require a schema-version change and a documented migration or compatibility note.
