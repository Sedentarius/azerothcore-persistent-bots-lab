# Manifests

Manifests connect code and configuration to actual server execution.

Planned categories:

- `releases/` - reproducible realm definitions;
- `sessions/` - start/stop windows and exposure;
- `experiments/` - optional generated links from experiments to runtime artifacts;
- `templates/` - canonical manifest templates.

A manifest records facts. Commentary and interpretation belong in experiment reports or incidents.
