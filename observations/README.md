# Observations

Use `OBS-*` records for findings from ordinary play or non-controlled server operation.

A useful observation includes:

- session ID;
- what was directly observed;
- supporting log/data reference if available;
- interpretation clearly separated from the observation;
- follow-up issue or experiment when needed.

Observations are valuable evidence for discovery and debugging. They are not automatically causal experiments.
