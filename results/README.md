# Results

Results summarize completed experiments. They do not replace datasets or analysis code.

Recommended result states:

- `supported`;
- `partially_supported`;
- `not_supported`;
- `inconclusive`;
- `invalidated`.

A result should link its experiment, release, sessions, dataset, and analysis. Report limitations next to the conclusion, not in a separate place readers may miss.
